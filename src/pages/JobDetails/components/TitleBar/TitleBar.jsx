import classes from "./TitleBar.module.css";

const TitleBar = () => {
  return (
    <div className={classes.titleBar}>
      <div>
        <h1>Job Details</h1>
      </div>
    </div>
  );
};

export default TitleBar;
